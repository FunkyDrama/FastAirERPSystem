# add this when staff_service are done


async def seed_staff_db(
    *,
    # settings: Settings,
    flush: bool,
    flights_count: int,
    days_window: int,
) -> None:

    print("[staff] seeding is skipped for now (no models wired yet)")
